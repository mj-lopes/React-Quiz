import { createTheme } from "@mui/material";

const MUIStyles = createTheme({
  palette: {
    primary: {
      main: "#42a5f5",
    },
    secondary: {
      main: "#ffc107",
    },
  },
});

export default MUIStyles;
